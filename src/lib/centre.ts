/**
 * Centre-selection constants, kept free of any server-only import so the
 * centre switcher (a client component) and the session reader (a server
 * module) can share them.
 */

export const CENTRE_COOKIE = "mr.centre";

/** The switcher's "all centres" position — the group-level view. */
export const ALL_CENTRES = "all";
